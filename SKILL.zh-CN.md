---

name: generate-courseware  
description: 根据课程主题、课程大纲、教学提纲、教案、培训目标或成人学习工作坊需求，创建视觉化、低文字密度、图片型中文教学课件。适用于 Codex 需要逐页规划教学内容、提供两种视觉风格供用户选择、使用 gpt-image-2 生成每一页 1920x1080 全页图片、组装纯图片 PowerPoint，并添加讲师备注的场景。

---

# 生成课程课件

## 核心规则

制作图片型教学课件，让抽象概念、复杂逻辑和隐藏结构变得可视化。不要制作大量 PPT 文本框堆叠的课件。不要使用 Python、python-pptx 或任何 Python PPT 工具来制作或组装最终课件。

面向学员的页面图片和最终 PowerPoint 中一律不得显示页码。页码只能作为课程规划、清单、文件名、提示词和讲师备注中的内部制作索引使用。封面、目录、章节过渡、内容、练习、总结和结束页均不得出现可见页码、页码标签、页码式页脚标记、页数进度或以装饰形式出现的页码。

除非用户明确要求修改、更新或修复本 skill，否则不要编辑 skill 文件本身。如果用户只是针对当前课件提出一次性偏好，只应用到当前任务，不要写入 `SKILL.md`。

默认流程如下：

1. 分析课程主题或大纲，并从 `references/visual-logic-library.md` 中选择合适的视觉逻辑结构。
2. 创建逐页课程规划，并等待用户确认后再生成最终图片。
3. 创建两种视觉风格方向供用户选择。每种方向都必须包含封面页、目录页和一页代表性内容页，尽量根据用户提供的内容进行制作，如果选用则可以直接留用到最终呢间中，避免二次制作浪费算力。
4. 用户选择风格后，锁定课件风格指南，并在后续页面中保持一致。尤其注意每页中的标题、正文、强调字等的字体、字号、字效分别都保持一致。
5. 使用 `gpt-image-2` 将每一页生成为独立的 1920x1080 全页图片，并确保最终页面图片包含准确的、面向学员的中文文字。
6. 检查每一页图片的可读性、视觉逻辑、文字密度、风格一致性、标题准确性和教学价值；对不合格页面进行重新生成或修复。对不合理和不应该出现在课件中的文字，则剔除。
7. 执行标题保真检查：标题必须准确、一致，且不能破坏插图。课题、讲师名只出现在封面中，不要重复到后续页面中。
8. 使用 PowerPoint 自动化将图片全铺插入 PPT，并写入讲师备注。

## 课程规划

在生成任何最终页面前，先创建 `courseware-output/<course-title>/course-plan.md`。

规划文件必须每页一行，并包含：

- 页码
- 页面角色，例如封面、钩子、概念、框架、对比、流程、演示、练习、清单或总结
- 页面标题
- 一个核心内容
- 视觉隐喻、图表类型或逻辑结构
- 页面文字限制
- 讲师备注草稿

普通正式课程默认使用 18-24 页，除非用户另有指定。短课可以更少；只有在大纲确实需要时才增加页数。如果遇到页数过多，则分步制作，比如每次5-8页，直至所有都做完再进行合并。除非用户明确说跳过确认，否则规划完成后要等待用户确认。

如果原来结构中没有过渡页，则在对应的位置自动补充过渡页设计。每个目录页中的小标题对应一个过渡页。

在规划页面视觉前，读取 `references/visual-logic-library.md`，并将抽象概念匹配到合适结构。可使用 SWOT、PEST、战略金字塔、增长飞轮、漏斗、矩阵、旅程图、商业画布、生命周期、RACI、泳道图、路线图、KPI 树或 AI 转型路线图等结构。

## 风格选择

制作完整课件前，在 `courseware-output/<course-title>/style-options/` 中创建两套完整风格方向：

```text
style-a-cover.png
style-a-toc.png
style-a-content.png
style-b-cover.png
style-b-toc.png
style-b-content.png
style-options.md
```

每套风格方向必须包含：

- 封面页样张（使用真实课程页面制作）
- 目录页样张（使用真实课程页面制作）
- 使用真实课程页面制作的一页代表性内容页样张
- 配色、字体感觉、标题位置、内容网格、图标风格、背景母题、卡片/面板风格，以及无页码的页脚处理方式；所有样张均不得出现可见页码

如果用户没有指定风格，默认将风格 A 设为 `references/visual-courseware-patterns.md` 中描述的蓝色科技商务咨询风格，灵感来自用户提供的蓝色科技管理图表 PDF。风格 B 做成与课程内容和行业更贴合的替代方向。除非用户明确说跳过风格选择，否则不要在用户选择风格前继续制作完整课件。

用户选择后，创建 `courseware-output/<course-title>/style-guide.md`，并在后续课件制作中保持稳定。风格指南必须锁定：

- 主色板和强调色
- 标题/副标题字号层级和固定位置
- 按页面角色划分的字体层级：
  - 封面标题：主视觉标题，视觉上应接近用户偏好的大标题封面参考。它必须占据第一视觉层级，并明显大于任何内容页标题。
  - 章节标题：稍大，但层级低于封面标题。
  - 内容页标题：中大字号，在所有内容页中保持一致，视觉上接近用户偏好的内容页参考。不能压过页面主体图示，也不能挤占图示空间。
  - 模块/卡片标签：明显小于内容页标题，并在整套课件中保持一致。
- 内容面板几何结构和边距
- 图标与插图风格
- 背景纹理或场景规则
- 禁止显示页码的规则、章节标签和无页码页脚的处理方式
- 图示、箭头、卡片、图表和标注规则

## 视觉设计

每一页必须通过视觉结构来教学或者展示。优先使用流程图、循环图、漏斗、矩阵、前后对比、清单、故事板、时间线、决策树、仪表盘、场景图和带标注案例。让抽象概念变得可见、有结构、容易记住。

## 语言语气

所有页面文字和讲师备注都使用平和、建设性、尊重学员的表达。

- 优先直接说明目标、下一步行动或收益。
- 避免重复使用对立式、冲击式口号，例如“不是……而是……”“别再……”以及否定学员现有做法的表达。
- 用“补充、改进、适用条件”的方式表达，不要把学员当前方法说成错误或过时。
- 提问要偏探索和实用，邀请学员贡献自己的行业经验，而不是直接灌输结论。

保持页面文字稀疏：

- 每页只表达一个核心信息。
- 使用短中文标签，不要写段落。
- 使用大标题和可读标签。
- 保持标题层级正确：封面标题必须是整套课件中最大的文字；内容页标题要小于封面标题，不要变成过大的海报式标题。
- 同类内容页的标题字号和位置要一致。标题较长时，优先压缩文字或拆分视觉重点，不要简单放大标题或让标题挤占图示空间。
- 避免过小 UI 文字、密集项目符号列表和装饰性文字块。
- 使用图标、箭头、数字、面板和空间关系表达逻辑。
- 风格确定后，保持标题大小、标题位置、面板节奏、边距系统、图标粗细、发光/阴影处理和页脚处理一致。
- 相同教学角色重复使用相同视觉语言：所有章节页要有家族感；所有框架页共享版式语法；所有练习页共享工作表语法。

选择风格或匹配用户过往样张时，读取 `references/visual-courseware-patterns.md`。为概念选择最佳结构时，读取 `references/visual-logic-library.md`。

## 图片生成

使用 `gpt-image-2` 独立生成每一页图片。

每一页最终交付物必须是一张完整的、面向学员的图片页，且可读、准确的中文文字已经烘焙进最终导出的 PNG。不要把无文字背景图当作完成页面交付或展示。

推荐的准确文字工作流：

- 使用 `gpt-image-2` 生成视觉页面/背景。
- 检查并保证页面中中文标题、标签、数字和短语的文字准确性，如果出现乱码或者错误，则重新生成该页面。
- 当所有页面完成并合并为PPT文件后，如果用户要求“remove text”或“去掉文字”，则去掉每张图片中的文字，但确保所有生成的其他视觉内容、构图、颜色和元素都不变。
- 当用户只要求去掉文字时，不要重新生成、裁切、遮挡或重新设计图片。

默认最终图片设置：

- 模型：`gpt-image-2`
- 尺寸：`1920x1080`
- 质量：`high`
- 比例：16:9
- 输出格式：尽可能使用 PNG

为每一页生成图片前，在 `page-prompts.md` 中写入页面提示词。每个提示词必须说明：

- 课程标题和受众
- 页码和页面角色
- 页面上应出现的准确中文文字
- 视觉隐喻、版式和层级
- 风格方向
- 目录页中的章节和同一页中的小标题，尽可能保证对账，或都采用同样字数
- 用户选择风格后的锁定风格指南细节
- 负面约束：不要长段落、不要不可读的小中文、不要额外虚构标签、不要扭曲 logo、不要水印；任何页面都不得显示页码、页码标签、页码式页脚标记或页数进度；不要出现给讲师看的提示；除了封面，不要在其他内页中再次出现课题和讲师名等信息。

生成页面保存为：

```text
courseware-output/<course-title>/pages/slide-01.png
courseware-output/<course-title>/pages/slide-02.png
```

## 质量门

组装前检查每一页生成图片。出现以下情况时必须重新生成该页：

- 中文文字错误、乱码、太小或太密。
- 封面标题太小，弱于内容页标题，或不是首屏最强视觉信号。
- 内容页标题过大，大于选定内容页标题参考，或挤压主体图示。
- 同类页面的标题字体、标题字号或正文标签字号不一致。
- 视觉没有表达预期逻辑。
- 页面像通用装饰图，而不是教学材料。
- 页面不符合选定风格指南。
- 重要内容被裁切、重叠或埋没。
- 风格一致性被破坏，且没有教学上的理由。
- 出现任何可见页码、页码标签、页码式页脚标记或页数进度。

在可用时使用 OCR 或视觉检查。如果准确文字很关键，减少图中文字并重新生成，直到文字干净。

## 标题保真检查

最终组装前，对照 `deck_manifest.json` 检查每一页可见标题。

- 封面标题必须准确，并且是整套课件中最大的文字。
- 内容页标题必须使用相同字体、字号、位置和标题区域处理方式。
- 如果生成标题出现错字、重影标题、混合字体风格或字号不一致，不要用大块不透明色带遮住画面。应按眼球重新生成。
- 不要使用 Python 或 Python PPT 工具完成这个修复或组装流程。

## 讲师备注

每一页必须包含如下格式的讲师备注：

```text
讲解目标：...

讲解要点：...

过渡提示：...
```

备注要服务于讲述，而不是让讲师照读。备注中应包含教学意图、讲师可以说的案例或提问，以及到下一页的过渡。

## 组装

根据 `references/manifest-schema.md` 创建 `deck_manifest.json`，然后运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/assemble_ppt.ps1 -ManifestPath "courseware-output/<course-title>/deck_manifest.json"
```

该脚本使用 Microsoft PowerPoint 自动化。它会创建 16:9 课件，把每张图片全铺插入页面，写入讲师备注，并保存 `.pptx`。

不要使用 Python、python-pptx 或其他 Python 工具来创建或组装 PowerPoint 课件。

如果 PowerPoint 自动化不可用，停止并告知用户。不要退回到 Python PPT 生成方案。
